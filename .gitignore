data/*.bak_prescrub

# 4K source frames handed to the thumbnail lane. Working files, not deliverables:
# 13 MB each, and publishing them would bloat a Pages repo for no reader benefit.
articles/*/cover-source-*.png
